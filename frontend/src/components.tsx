import React from 'react';export const Card=({children,className=''})=><div className={'card '+className}>{children}</div>; export const Badge=({children,tone='blue'})=><span className={'badge '+tone}>{children}</span>; export const Btn=({children,onClick,className=''})=><button onClick={onClick} className={'btn '+className}>{children}</button>; export function Empty({text='Sin datos'}){return <div className="empty">{text}</div>}
